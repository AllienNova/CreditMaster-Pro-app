# backend-builder — Memory Index

- [Scoped stash in a shared worktree](feedback_scoped-stash-shared-worktree.md) — use `git stash push -- <path>`, never a bare stash, to prove red-before-green without disturbing other agents.
- [tax_profiles schema drift beyond tax_accounts](project_tax-profiles-schema-drift.md) — ~20 more columns mapDatabaseToProfile reads don't exist live; same defect class, flagged not fixed.
- [test:coverage:changed is branch-wide, not session-wide](project_coverage-changed-gate-branch-wide.md) — grep the log for your own file paths; the aggregate count includes the whole branch vs. main.
- [Jest resetMocks + postgrest-js error-vs-throw](feedback_jest-resetmocks-and-postgrest-errors.md) — re-apply createClient mocks in beforeEach every time; supabase queries resolve {error}, never throw on DB errors.
- [GDPR erasure RPC broken](project_gdpr-erasure-cascade-broken.md) — delete_user_data_cascade fails on its own first INSERT (audit_logs.resource_type NOT NULL); unfixed, flagged to compliance owner.
- [budgets quarterly CHECK constraint gap](project_budgets-quarterly-check-constraint-gap.md) — budgets_period_check omits 'quarterly' though the type/route/UI all accept it; unfixed, needs a migration.
- [Grep repo-wide for fictional fixtures](feedback_grep-repo-wide-for-fictional-fixtures.md) — a schema-mismatch bug's wrong column names are often duplicated as mock fixtures in other test files too, not just the assigned one.
- [Pathspec-scoped commit with a shared index](feedback_pathspec-scoped-commit-shared-index.md) — `git commit -- <your paths>` only isolates *other* files; if another agent co-edited the *same* file, it still sweeps in their hunks — extract yours via `git apply --cached` on a hand-built patch first.
- [git rm for paired source+test deletion](feedback_git-rm-for-paired-source-test-deletion.md) — jail-guard blocks bare `rm` on test files; `git rm` isn't caught and is the legitimate path when the test's own dead module is deleted in the same commit.
- [Route deletion leaves stale generated artifacts](feedback_route-deletion-stale-generated-artifacts.md) — rebuild `.next` locally (safe, gitignored), but don't wholesale-regenerate a shared checked-in generated file (e.g. openapi spec) mid-wave — it pulls in everyone else's concurrent route changes.
- [Thenable mock chain for postgrest-js](feedback_thenable-mock-chain-for-postgrest.md) — one mock object with its own `.then()` correctly models every Supabase call shape (`.single()`, `.in()`, `.order()`, or awaited bare after `.eq()`) instead of guessing "the last method."
- [financial-chat-engine.ts fabricated advice, fixed](project_fabricated-advice-financial-chat-engine.md) — phantom `portfolio_holdings`/`financial_accounts` tables fixed 2026-07-31; real `investment_holdings`/`debt_accounts` column facts + 2 unfixed sibling bugs (plaid-service.ts, portfolio/analyze/route.ts) documented.
