---
name: jest-resetmocks-and-postgrest-errors
description: Recurring gotchas when writing/testing Supabase persistence code in this repo — jest resetMocks wiping jest.mock() factories, postgrest-js resolving {error} instead of throwing, and the established PGRST116 not-found convention.
metadata:
  type: feedback
---

**1. jest.config.js sets `resetMocks: true` globally.** This wipes the implementation of every `jest.fn()` — including ones created inside a `jest.mock("@/lib/supabase/server", () => ({ createClient: jest.fn(...) }))` factory — before every single test, not just call history. Any test file mocking `createClient()` MUST re-apply the implementation in its own `beforeEach` (cast the imported mock to `jest.Mock` and call `.mockImplementation(...)` again each time), or `createClient()` silently resolves to `undefined` from the second test onward.

**Why:** Found 2026-07-31 while fixing `src/lib/trading/positions/__tests__/position-manager.test.ts` (orders/positions persistence task). Its `resetChainMocks()` helper reset the chain method mocks (`select`/`eq`/`upsert`/...) but never re-applied `createClient`'s own implementation. 19 of 26 tests in that file were passing by accident — `createClient()` returned `undefined`, `supabase.from(...)` threw `TypeError: Cannot read properties of undefined (reading 'from')`, and the old code's blanket try/catch swallowed it silently. The bug was invisible until the try/catch was removed (to stop swallowing real persistence failures) and the tests started failing loudly. `src/lib/trading/__tests__/order-manager.test.ts` already had the correct pattern (`mockedCreateClient.mockImplementation(...)` inside its own `beforeEach`, with a comment explaining why) — that file is the reference to copy from.

**How to apply:** Before writing or trusting any new jest test that mocks `@/lib/supabase/server`'s `createClient`, confirm the mock's implementation is re-applied in `beforeEach`, not just declared once at module scope. A test that "passes" on a mock returning `undefined` is not exercising the code path it claims to.

**2. postgrest-js resolves `{ data, error }` — it does not throw on a DB/Postgrest-level error.** Confirmed by reading `node_modules/@supabase/postgrest-js/src/PostgrestBuilder.ts`: `.then()` only rejects on a genuine network/fetch failure (and even that gets caught and re-resolved as `{error, data: null, ...}` unless `.throwOnError()` was explicitly chained). This means a bare `try { await supabase.from(x).upsert(y); } catch { /* swallow */ }` catches essentially nothing related to the actual DB — a relation-does-not-exist error, a constraint violation, an RLS denial all come back as a resolved `{error}`, invisible unless the code destructures and checks it.

**Why:** Root cause of the original defect this task fixed — `order-manager.ts`/`position-manager.ts` never even read `{error}` off `.upsert()`'s result, so every write to the (nonexistent) `orders`/`positions` tables silently no-op'd for the entire history of those files, and the surrounding try/catch never had anything to catch.

**How to apply:** When writing or reviewing ANY Supabase persistence code in this repo, the checklist is: destructure `{ error }` from every `.upsert()`/`.insert()`/`.update()`/`.select()` call, check it explicitly, and decide deliberately whether to throw/log/return based on whether silent failure would mean data loss (write paths) or a misleading empty result (list/read paths) — never rely on an ambient try/catch to catch a DB error, because it won't. See [[project-gdpr-erasure-cascade-broken]] for a second instance of a swallowed/misfired error path found via this same lens.

**3. This codebase already has an established convention for the `.single()` not-found case: `error.code === "PGRST116"`.** Confirmed by grep — used in `src/lib/commerce/offers/offer-service.ts`, `src/lib/commerce/affiliate/{tracking,commission-calculator,affiliate}-service.ts`, `src/app/api/{settings,profile,onboarding/progress}/route.ts`, and several more. The idiom is `if (error) { if (error.code === "PGRST116") return null; throw new Error(...); }` — PGRST116 ("JSON object requested, multiple (or no) rows returned") is PostgREST's genuine not-found signal for a `.single()` query; any other code is a real failure (permission, connection, malformed query) that must throw, not return null. Applied this to `budget-service.ts`'s `getBudgetById` (commit `89718c2`, 2026-07-31) — it previously did `if (error || !data) return null`, silently misreporting e.g. a `42501 permission denied` as "budget not found." Match this exact idiom rather than inventing a new one when fixing similar swallowed-error patterns elsewhere in the codebase.

**4. A bare `throw error;` on a destructured PostgREST `{error}` throws a PLAIN OBJECT (`{message, code, details, hint}`), not an `Error` instance.** A downstream `catch (e) { e instanceof Error ? e.message : "Unknown error" }` — the standard pattern used throughout this codebase for turning a caught value into a string — silently falls through to the fallback string and loses the real DB error message, because `instanceof Error` is `false` for that object.

**Why:** Found 2026-07-31 fixing `credit-bureau-service.ts`'s `submitDispute`. Its private `saveDisputeRecord` helper does `if (error) throw error;` (rethrows the raw object). My new outer catch used the codebase's standard `instanceof Error` idiom to build a `persistenceError` string for operator reconciliation — a test asserting that string contained the real Postgres message failed with `"Unknown persistence error"` instead, because the thrown value was never an `Error`.

**How to apply:** When writing a catch block that must preserve the real message from a value that MIGHT be a rethrown raw PostgREST error (check the function you're catching from for a bare `throw error` vs. `throw new Error(...)`), don't rely on `instanceof Error` alone. Use a small helper that also accepts `{message: string}`-shaped objects — see `extractErrorMessage()` in `credit-bureau-service.ts` for the pattern. This matters most for error paths whose message is actually surfaced somewhere (logs, reconciliation fields, API responses) — if the message is only ever swallowed silently anyway, the gap won't be noticed, which is exactly why it survived undetected until a test asserted on the string content.

**6. Gotcha #1 generalizes past `createClient` — it wipes ANY `jest.mock(...)` factory's `.mockImplementation`, including a mocked class constructor, and EARLIER tests in the same file can mask it.** Fixing `/api/investments/portfolio/analyze/route.ts`'s GET handler (phantom `cost_basis`/`shares`/`asset_class` columns), a new regression test constructing `PortfolioAnalysisService` via the route's real code path threw `TypeError: portfolioService.analyzePortfolio is not a function` — `new PortfolioAnalysisService()` returned `{}` (empty), not the mocked `{analyzePortfolio, analyzeDiversification}` shape the top-of-file `jest.mock("@/lib/investments/services/PortfolioAnalysisService", () => ({ PortfolioAnalysisService: jest.fn().mockImplementation(() => ({...})) }))` factory declared. Confusingly, an EARLIER test in the same file ("returns 200 for a fully valid body", POST) constructs the exact same mocked class successfully — debug logging (`Object.keys(portfolioService)`) proved POST's construction has the real shape while GET's (running later) is empty `{}`, on the same mock, same file, same run. Never fully root-caused *why* one construction survives and a later one doesn't (plausibly some jest-circus timing quirk in exactly when the per-config auto-reset hook fires relative to module-factory evaluation) — but a test passing by dumb luck on construction #1 and breaking on construction #N is worse than useless, since it will pass in isolation (`-t "test name"`) and fail only as part of the full suite, which is exactly what happened here.

**Why:** A test file with 8 pre-existing, already-green tests provided false confidence that the `PortfolioAnalysisService` mock was reliably configured — until a 9th test (mine) constructed it from a code path (`GET`, not `POST`) none of the earlier tests exercised, and only running the *whole file* (not `-t`-filtered) exposed the gap.

**How to apply:** Don't trust that a class mock declared once via a `jest.mock(...)` factory stays live for a test you're adding, even if sibling tests using "the same" mock pass — construction order and code path matter under `resetMocks`/`restoreMocks: true`. Re-apply `SomeClass.mockImplementation(() => ({...}))` explicitly inside the new test's own `beforeEach` (fetch it via `jest.requireMock(...) as { SomeClass: jest.Mock }`, matching the `getPortfolioAnalyticsMock()` idiom already used in `src/app/api/investments/analytics/*/​__tests__/route.test.ts`), exactly as gotcha #1 prescribes for `createClient`. Always run a new/changed test as part of its **full file**, not filtered by `-t`, before trusting it green — a filtered run can hide exactly this class of order-dependent breakage.

**5. `jest.config.js` ALSO sets `restoreMocks: true` (in addition to `resetMocks`) — this fully un-spies any `jest.spyOn(obj, 'method')` before every test, not just clearing its implementation.** A `const spy = jest.spyOn(console, "error").mockImplementation(...)` declared once at `describe`-scope gets detached from `console.error` before test #2 even runs: the REAL `console.error` executes (visible as raw printed output in the test log) and the stale `spy` reference shows `Number of calls: 0` in assertions — a confusing "it printed but wasn't recorded" symptom, distinct from #1's "silently resolves to `undefined`" symptom for plain `jest.fn()` factories.

**Why:** Found 2026-07-31 writing `src/app/api/privacy/_lib/__tests__/audit.test.ts` — a `consoleErrorSpy` created once outside `beforeEach` passed its first two tests (which only asserted `.not.toHaveBeenCalled()`, trivially true once detached) then failed the next two (which asserted `.toHaveBeenCalledWith(...)`) with the exact message visibly printed to the terminal by the real `console.error` one line above the failing assertion.

**How to apply:** Any `jest.spyOn` on a real object method (not a module-level `jest.mock` factory) must be re-created fresh inside `beforeEach` — declare the variable with `let` at `describe`-scope, assign via `spy = jest.spyOn(...).mockImplementation(...)` inside `beforeEach`, not `const` at declaration time. Re-applying `.mockImplementation()` alone on the stale reference (the fix for gotcha #1) is NOT sufficient here — the reference itself is stale.
