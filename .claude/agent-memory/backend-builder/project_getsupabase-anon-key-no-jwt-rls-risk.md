---
name: getsupabase-anon-key-no-jwt-rls-risk
description: getSupabase() (src/lib/supabase/client.ts) builds its client with only the anon key and no forwarded user session/JWT — CONFIRMED (2026-07-31, two independent checks) it fails LOUD (42501/grant-denied), not silently empty. Root cause is a GRANT-level lockout, not just NULL auth.uid(). Architecture concern, not a data-integrity defect — still not fixed.
metadata:
  type: project
---

`getSupabase()` in `src/lib/supabase/client.ts` calls `createSupabaseClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY)` with no session, no cookie forwarding, and no `Authorization` header override. It is a bare anon-key client. Every server-side service that does `const supabase = getSupabase();` at module scope (confirmed: `credit-bureau-service.ts`, `credit-monitoring-service.ts`, and by pattern likely most of `src/lib/**`) therefore talks to Postgres as the unauthenticated `anon` role — `auth.uid()` resolves to `NULL` inside any RLS policy evaluated on those connections, regardless of which end user actually triggered the request.

This matters because the established RLS idiom across this schema (`disputes`, `credit_accounts`, `bureau_connections`, the `bureau_disputes`/`credit_alerts` tables added in commit `2bc57fd`, and many more) is `USING (auth.uid() = user_id)` / `WITH CHECK (auth.uid() = user_id)`. If `auth.uid()` is genuinely always NULL on these connections, every one of those policies silently blocks the exact writes the application code performs — `NULL = user_id` is `NULL`, not `TRUE`. A working production app strongly implies this is NOT actually happening (either something forwards the JWT that this file's Proxy pattern doesn't make obvious, or these specific tables/policies are more decorative than load-bearing at runtime and something else is doing the real access control) — but I could not find the mechanism in the time this task allowed, and did not chase it further since it is a cross-cutting, pre-existing concern spanning dozens of files, not specific to the two bugs I was assigned.

Contrast: `src/lib/supabase/server.ts` and `src/lib/supabase/admin.ts` both exist and use `SUPABASE_SERVICE_ROLE_KEY` (bypasses RLS entirely). Some tables in this schema (`bill_alerts`, `emotional_spending_alerts`) have NO insert RLS policy at all — meaning their writes were clearly designed to go through a service-role connection, not the anon-key `getSupabase()`. That split (some services assume anon-key + forwarded JWT, others assume service-role bypass, with no obvious rule for which is which) is itself worth someone auditing.

**Why:** Discovered while designing RLS for `bureau_disputes`/`credit_alerts` (docs/qa/SYSTEMATIC-REVIEW-SYNTHESIS.md findings #6/#7) — needed to decide whether an INSERT policy was load-bearing or decorative for tables written exclusively through `credit-bureau-service.ts`/`credit-monitoring-service.ts`'s `getSupabase()` calls, and could not confirm from the code whether `auth.uid()` is ever non-NULL on that client.

**How to apply:** Do not assume an `auth.uid() = user_id` RLS policy is actually the enforcement mechanism for a table written through `getSupabase()` (`@/lib/supabase/client`) without verifying JWT forwarding first — grep for where the connection's `Authorization` header or session gets set, or run a real integration test against local Supabase with `NODE_ENV` matching production request handling. If asked to harden auth/authz on a route whose service uses `getSupabase()`, this is the first thing to check, not the RLS policy text. Related: [[feedback_jest-resetmocks-and-postgrest-errors]] (same service layer, adjacent Supabase-client gotchas).

**CONFIRMED, twice, 2026-07-31 — this is fail-LOUD, not fail-silent, so it is NOT the same defect class as the phantom-table swallows.** (1) `docs/qa/SYSTEMATIC-REVIEW-SYNTHESIS.md` §B: `GET /rest/v1/transactions` with the anon key and no user JWT returns `42501 "permission denied for table transactions"` against the live stack — the opposite of a silent empty result. (2) Independently re-verified via `information_schema.role_table_grants` on the local Postgres: `anon` and `authenticated` hold only `REFERENCES/TRIGGER/TRUNCATE` on `budgets` and `transactions` — **zero SELECT/INSERT/UPDATE/DELETE grant**, confirmed identical for both roles and both tables. This is the actual root cause, and it's a GRANT-level lockout: PostgREST rejects the request before RLS policy text is even evaluated, so `auth.uid()` being NULL is a red herring — the request never gets that far. Every migration in `supabase/migrations/` that adds a table+RLS-policy pair inherits this same silent-until-called gap; it hasn't caused visible failures because nothing has yet exercised a `getSupabase()`-backed write against a fresh RLS-only table in an environment that isn't service-role. `src/lib/supabase/client.ts`'s own comment and `budget-service.ts:393`'s "KNOWN REMAINING GAP" comment both independently corroborate this is already-known, deliberately-deferred, cross-cutting, and explicitly out of scope for any single phantom-table fix — do not attempt to fix it as a side effect of an unrelated table/RLS change; it needs one dedicated pass across the whole `getSupabase()` call-site set (dozens of files) or a platform-level GRANT fix, not a per-table patch.
