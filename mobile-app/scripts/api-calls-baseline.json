{
  "frozen": "2026-08-15",
  "broken": [
    "/api/notifications/register",
    "/api/notifications/unregister",
    "/credit/reports/ ",
    "/credit/scores/refresh",
    "/credit/simulate",
    "/disputes/ /generate",
    "/documents/ /analyze",
    "/documents/ /download",
    "/identity/alerts",
    "/identity/alerts/ /resolve",
    "/identity/dark-web/disable",
    "/identity/dark-web/enable",
    "/identity/scan",
    "/identity/status",
    "/notifications/push-token",
    "/tax/calendar/ ",
    "/tax/calendar/ /complete",
    "/tax/calendar/ /reminder",
    "/tax/compare",
    "/tax/documents/ /download",
    "/tax/scenarios",
    "/tax/scenarios/ ",
    "/user/billing/payment-method",
    "/user/recommendations",
    "/user/recommendations/ /click",
    "/user/recommendations/ /dismiss",
    "/user/settings/2fa/disable",
    "/user/settings/2fa/enable",
    "/user/settings/2fa/verify"
  ],
  "verbMismatch": [
    "DELETE /financial/accounts/ ",
    "GET /tax/deductions/recommendations",
    "PATCH /notifications/preferences",
    "POST /notifications/preferences",
    "POST /student-loans",
    "POST /tax/calendar"
  ],
  "brokenWhy": {
    "/api/notifications/register": "Expo push token registration. No Expo push path exists server-side, and push_subscriptions is web-push shaped (endpoint + keys_p256dh + keys_auth) — an ExponentPushToken has nowhere to go. Same blocker as /notifications/push-token. ALSO double-prefixed (/api on top of the client's base URL); not corrected because the un-prefixed path 404s too, and changing it would imply a route that does not exist. Added by hand, NOT via --write-baseline, which is shrink-only: these are pre-existing breakage that became visible when the scanner learned to match apiClient.* calls, not new breakage.",
    "/api/notifications/unregister": "Expo push token registration. No Expo push path exists server-side, and push_subscriptions is web-push shaped (endpoint + keys_p256dh + keys_auth) — an ExponentPushToken has nowhere to go. Same blocker as /notifications/push-token. ALSO double-prefixed (/api on top of the client's base URL); not corrected because the un-prefixed path 404s too, and changing it would imply a route that does not exist. Added by hand, NOT via --write-baseline, which is shrink-only: these are pre-existing breakage that became visible when the scanner learned to match apiClient.* calls, not new breakage.",
    "/notifications/push-token": "Expo push token registration; no Expo send path server-side and push_subscriptions is web-push shaped. Blocked on a product decision: add Expo push, or move the app to web-push.",
    "/disputes/ /generate": "BUILDABLE, needs care. Generate an AI letter for an EXISTING dispute. Verified: public.disputes carries bureau/item_type/item_description/reason/creditor_name/account_number/balance/inaccuracy_type plus letter_content to store the result, and profiles carries full_name/address/city/state/zip for the userInfo{name,address} that handleAIGeneration requires. Reachable from disputeStore.generateAILetter whenever a dispute is selected (it falls back to generateFromTemplate otherwise). THREE THINGS THE BUILD MUST GET RIGHT: (1) /api/disputes/generate mode:ai CREATES a dispute; this route must UPDATE the existing one's letter_content instead. (2) It deducts credits via creditService.deductCredits — a per-dispute route that skips that is a free path around billing. (3) If the profile has no name or address, answer 400 and say so; a dispute letter with a placeholder return address is worse than no letter.",
    "/tax/calendar/ ": "BLOCKED on a table. /api/tax/calendar COMPUTES its events from the user's tax profile — its own docblock says 'There is no tax_calendar_events table' — so the events it returns have no persistent id. Nothing can be completed or given a reminder until they are stored.",
    "/tax/calendar/ /complete": "BLOCKED on a table. /api/tax/calendar COMPUTES its events from the user's tax profile — its own docblock says 'There is no tax_calendar_events table' — so the events it returns have no persistent id. Nothing can be completed or given a reminder until they are stored.",
    "/tax/calendar/ /reminder": "BLOCKED on a table. /api/tax/calendar COMPUTES its events from the user's tax profile — its own docblock says 'There is no tax_calendar_events table' — so the events it returns have no persistent id. Nothing can be completed or given a reminder until they are stored.",
    "/tax/compare": "BLOCKED on bracket data. Year-over-year comparison; app/tax/optimizer.tsx asks for 2024/2025/2026. FEDERAL_TAX_BRACKETS_2024 is the only bracket table in the codebase, and /api/tax/brackets already refuses any other year for exactly this reason. Building it would either apply 2024 brackets to three years or 400 on every call the app makes. Unblocker: real IRS tables for the other years.",
    "/tax/scenarios": "BLOCKED on a table. /api/tax/scenarios/{calculate,compare} exist and are stateless; saving and listing scenarios needs somewhere to put them. No scenarios table exists.",
    "/tax/scenarios/ ": "BLOCKED on a table. /api/tax/scenarios/{calculate,compare} exist and are stateless; saving and listing scenarios needs somewhere to put them. No scenarios table exists.",
    "/identity/alerts": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/identity/alerts/ /resolve": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/identity/dark-web/disable": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/identity/dark-web/enable": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/identity/scan": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/identity/status": "BLOCKED on schema AND a vendor. No identity-protection or dark-web table exists, and dark-web scanning is a third-party capability this codebase has no integration for. Schema proposed in docs/specs/adr/0013-credit-freeze-and-identity-theft-storage.md (Status: Proposed, D1-D3 unanswered) — the same ADR that blocks six web calls.",
    "/user/recommendations": "BLOCKED on a table. No user_recommendations table exists. tax_recommendations is a different thing (tax-year scoped, already served by /api/tax/recommendations) and financial_insights is not click/dismiss-tracked. Needs a schema decision before a route can return or record anything.",
    "/user/recommendations/ /click": "BLOCKED on a table. No user_recommendations table exists. tax_recommendations is a different thing (tax-year scoped, already served by /api/tax/recommendations) and financial_insights is not click/dismiss-tracked. Needs a schema decision before a route can return or record anything.",
    "/user/recommendations/ /dismiss": "BLOCKED on a table. No user_recommendations table exists. tax_recommendations is a different thing (tax-year scoped, already served by /api/tax/recommendations) and financial_insights is not click/dismiss-tracked. Needs a schema decision before a route can return or record anything.",
    "/user/settings/2fa/enable": "BLOCKED on a design decision — see docs/specs/security-findings.md SF-08. MFAService runs on a session-less anon singleton so its TOTP methods cannot work server-side, and the obvious fix (a per-request client with an Authorization header) does NOT work: verified from auth-js source, the MFA methods read the client's stored session and pass it as `jwt`, ignoring global headers. Disable additionally needs aal2, so it must challenge+verify before unenrolling.",
    "/user/settings/2fa/verify": "BLOCKED on a design decision — see docs/specs/security-findings.md SF-08. MFAService runs on a session-less anon singleton so its TOTP methods cannot work server-side, and the obvious fix (a per-request client with an Authorization header) does NOT work: verified from auth-js source, the MFA methods read the client's stored session and pass it as `jwt`, ignoring global headers. Disable additionally needs aal2, so it must challenge+verify before unenrolling.",
    "/user/settings/2fa/disable": "BLOCKED on a design decision — see docs/specs/security-findings.md SF-08. MFAService runs on a session-less anon singleton so its TOTP methods cannot work server-side, and the obvious fix (a per-request client with an Authorization header) does NOT work: verified from auth-js source, the MFA methods read the client's stored session and pass it as `jwt`, ignoring global headers. Disable additionally needs aal2, so it must challenge+verify before unenrolling."
  },
  "verbMismatchWhy": {
    "_what": "The PATH resolves, so the path check passes; the handler it resolves to does not export this METHOD, so Next.js answers 405 at runtime and the calling screen swallows it. Shrink-only, like broken[].",
    "_lost_once": "These reasons were written on 2026-08-17 and then silently deleted by `audit:api --write-baseline`, which rebuilt the file from three fields. The writer now carries `why` and `verbMismatchWhy` through every rewrite.",
    "GET /tax/deductions/recommendations": "No recommendations route exists, so the call falls through to tax/deductions/[id] with id=\"recommendations\", which exports only PATCH and DELETE. The siblings categories/ and summary/ DO exist with GET; this one was never built. Belongs with the tax-deductions work (tasks #58/#60).",
    "POST /student-loans": "Creating a loan. /api/student-loans exports GET only. Blocked on the same schema decision as the web Add Loan form: student_loans.account_number is NOT NULL and ADR-0013 says store a mask, not a full account number.",
    "POST /tax/calendar": "SCHEMA-BLOCKED, not a missing handler — correcting an earlier note of mine that called this straightforward. createReminder (src/services/api/tax.ts:224) posts a TaxEvent to create a CUSTOM reminder and there is nowhere to put one: the tax_* tables are deductions, documents, profiles, recommendations and strategies, and a search for any event/calendar/deadline table returns only analytics_events, circuit_breaker_events, kill_switch_events, monitoring_events, processed_webhook_events and revenue_events — none a user reminder store. GET does not read a table at all; it COMPUTES the schedule from taxOptimizationEngine.getQuarterlyPaymentSchedule(profile). A POST that answered 200 and stored nothing would be the student-loans Add form defect again. Needs a table decision, in the shape of ADR-0013.",
    "PATCH /notifications/preferences": "NOT a verb typo — do not simply flip it to PUT. The route exports GET and PUT, but mobile types.ts NotificationPreferences carries categories:{disputes,scoreChanges,alerts,recommendations,payments,marketing} while the server model is channels:{dispute_update,score_change,payment_reminder,document_processed,recommendation,system,promotion}, and notification_preferences has NO categories column. Changing the verb alone would persist pushEnabled/emailEnabled/smsEnabled and SILENTLY DROP every category toggle. Exactly the shape proven live on the budgets route: PATCH {amount:750} returned 200 and left the amount at 500.00. Design written up in docs/specs/adr/0014-notification-taxonomy.md (Status: Proposed) — needs D1 (which taxonomy is canonical) and D2 (is the per-category channel matrix real) answered.",
    "POST /notifications/preferences": "app/settings/notification-preferences.tsx sends {categories:[{id,enabled,channels}], quietHours} — a per-category channel MATRIX. The server stores a flat channels object and a quiet_hours object, with no categories column at all. Same reconciliation as the PATCH entry. Design written up in docs/specs/adr/0014-notification-taxonomy.md (Status: Proposed) — needs D1 (which taxonomy is canonical) and D2 (is the per-category channel matrix real) answered.",
    "_disputesFixed": "POST /disputes/recommend-strategy closed 2026-08-17. recommendStrategy() already existed in @/lib/disputes/advanced-strategies and was already used by /api/disputes/generate; only the route was missing.",
    "_taxonomy": "FIVE notification taxonomies exist in this codebase (push-notification-service, web-push-service+scheduler, the preferences route, mobile types.ts, the mobile settings screen). Only score_change and payment_reminder appear in more than two. notification-scheduler.ts:461 gates delivery on keys the preferences route never writes, so the check is broken at the taxonomy level, not just the transport level. notification_preferences has 0 rows — nobody has ever saved one. See ADR-0014.",
    "DELETE /financial/accounts/": "Disconnecting a bank account (financial.ts:709). The detail route now exists with GET, so this moved from a path break to a verb break — a more accurate description of the same gap. DELETE is deliberately unbuilt: unlinking a Plaid-linked account is destructive with no established pattern here. manual-account-service.deleteAccount() works on `manual_accounts`, a different table, so it is not this. Deleting a financial_accounts row orphans its transactions and discards balance history; the alternative — mark disconnected and revoke the Plaid item — needs a column that does not exist. A DELETE that quietly destroys account history, or one that returns 200 and leaves the account linked, are both worse than the 404 today. Needs a decision on unlink semantics plus, probably, a status column."
  }
}
